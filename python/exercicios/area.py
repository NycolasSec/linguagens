# Area retangulo
base_retangulo = float(input("Qual a base do seu retangulo ? "))
altura_retangulo = float(input("Qual a altura do seu retangulo ? "))
area_retangulo = base_retangulo * altura_retangulo
print("A area do seu retangulo equivale a " + str(area_retangulo))
